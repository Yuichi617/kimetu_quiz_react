import React, { Component } from 'react';

class Header extends Component {
     render(){
         return(<header className="site-header">
             <div className="site_name"> <span className="oni">鬼</span>滅力診断 </div>
         </header>);
     }
}

export default Header;